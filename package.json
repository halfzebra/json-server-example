{
  "name": "json-server-example",
  "version": "0.0.1",
  "description": "Example project with json-server API mock",
  "main": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch data/db.json --port 3004"
  },
  "keywords": [ "example" ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.8.16"
  }
}
